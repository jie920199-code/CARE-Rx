# CARE-Rx Agent Rules

## 临床安全硬约束

1. 本项目是临床决策支持系统，不得表述为自动诊断或替代医生、康复医师、治疗师判断。
2. 红旗筛查、禁忌证和暂停/转诊安全门优先于任何处方匹配；规则引擎、界面和 AI 均不得绕过安全门。
3. 处方必须由有权限的治疗师审核并签发后才可生效或展示给护工执行。
4. 不得让 AI 自由生成治疗项目、剂量、禁忌证、进阶或降级标准。AI 如后续启用，只能转换或总结已经批准的结构化内容，且输出仍需审核。
5. 未明确或缺乏批准依据的临床内容必须标注“待临床审核”，不得自行猜测。

## 数据与隐私

1. 禁止提交真实患者姓名、身份证号、联系方式、病历、影像、照片、视频或可重识别信息。
2. 测试数据只能是虚构数据或经批准且彻底匿名化的数据；默认优先使用虚构病例。
3. 不得把本地患者数据库、备份、日志导出或密钥提交到 Git。

## 规则与变更控制

1. 临床规则、处方数据和页面代码必须分离，并具有稳定 ID、版本、状态和审计信息。
2. 每次修改临床规则、处方或安全门，必须新增或更新相应测试，包括安全阻断和边界病例。
3. 不得静默覆盖已签发版本；采用不可变版本或显式修订，并保留生成、审核和修改记录。
4. 规则冲突时采用：紧急红旗/转诊 > 禁忌/暂停 > 降级 > 允许匹配 > 优先级排序。未能确定时进入人工复核，不自动签发。

## 完成定义

完成任务前必须运行与改动相称的格式检查、Schema 校验、类型检查和测试；无法运行时必须说明原因。不得通过删除或放宽安全测试来使检查通过。
